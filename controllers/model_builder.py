from keras import Sequential
import keras.layers as layers

class ModelBuilder:
    def build_sequential_model(self, layers_list: list) -> Sequential:
        layers.
        if self.model_name == 'model1':
            return Model1()
        elif self.model_name == 'model2':
            return Model2()
        else:
            return None